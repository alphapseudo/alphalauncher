// Generated by AlphaLauncher on {{ datetime }}
// Manual changes here will be overwritten by the launcher
// See https://community.bistudio.com/wiki/server.armaprofile#Arma_3

difficulty="{{current}}";

class DifficultyPresets
{
  class CustomDifficulty
  {
    class Options
    {
      /* Simulation */

      reducedDamage = {{ boolToInt reducedDamage }};

      /* Situational awareness */

      groupIndicators = {{ groupIndicators }};
      friendlyTags = {{ friendlyTags }};
      enemyTags = {{ enemyTags }};
      detectedMines = {{ detectedMines }};
      commands = {{ commands }};
      waypoints = {{ waypoints }};
      tacticalPing = {{ boolToInt tacticalPing }};

      /* Personal awareness */

      weaponInfo = {{ weaponInfo }};
      stanceIndicator = {{ stanceIndicator }};
      staminaBar = {{ boolToInt staminaBar }};
      weaponCrosshair = {{ boolToInt weaponCrosshair }};
      visionAid = {{ boolToInt visionAid }};

      /* View */

      thirdPersonView = {{ boolToInt thirdPersonView }};
      cameraShake = {{ boolToInt cameraShake }};

      /* Multiplayer */

      scoreTable = {{ boolToInt scoreTable }};
      deathMessages = {{ boolToInt deathMessages }};
      vonID = {{ boolToInt vonID }};

      /* Misc */

      mapContent = {{ boolToInt mapContent }};
      autoReport = {{ boolToInt autoReport }};
      multipleSaves = {{ boolToInt multipleSaves }};
    };

    aiLevelPreset = {{ aiPreset }};
  };
 
  class CustomAILevel
  {
    skillAI = {{ skill }};
    precisionAI = {{ precision }};
  };
};
